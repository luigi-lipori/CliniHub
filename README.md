## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`

# 2. Virtualenv do backend
cd server
python -m venv env
source env/bin/activate
pip install -r requirements.txt
cd ..

# 3. Rodar
./start.sh
